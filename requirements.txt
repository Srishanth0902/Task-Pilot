# Google Calendar integration

# Google Calendar API client
google-api-python-client==2.199.0

# HTTP transport adapter used by the Google API client
google-auth-httplib2==0.4.1

# OAuth 2.0 "installed app" flow (browser consent + token caching)
google-auth-oauthlib==1.4.0

# Loads settings from a .env file
python-dotenv==1.2.3

# Week 2: validated schemas, LangChain tools, and OpenRouter-compatible chat models.
pydantic==2.13.5
langchain==1.4.0
langgraph==1.2.11
langchain-openai==1.6.2

# Week 5: HTTP API, server, and conversational web UI.
fastapi==0.141.1
uvicorn==0.52.4
streamlit==1.63.0
httpx==0.28.1
requests==2.34.2

# IANA timezone database for Windows and other systems without OS tzdata.
tzdata==2026.2
