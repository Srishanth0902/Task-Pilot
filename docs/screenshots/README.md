# Screenshot guidance

Before adding a public screenshot, use a demonstration calendar and check that
the image contains no personal email address, event title, meeting link,
location, attendee, or notification. Name reviewed assets descriptively, for
example `public-empty-state.png`; local `task-pilot*.png` capture attempts
are ignored by default.
